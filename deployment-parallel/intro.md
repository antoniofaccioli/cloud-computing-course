## Welcome to Exercise B

This exercise follows the same concepts as Exercise A but introduces the **NodePort** Service type, which exposes an application outside the cluster.

At the end of each step you will find a **Kahoot question**. Answer it on Kahoot before moving to the next step.

**Environment:** a single-node Kubernetes cluster is ready. All manifest files are in `/root/`.
