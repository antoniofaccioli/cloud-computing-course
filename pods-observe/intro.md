# Pods — observe and answer

A Kubernetes cluster is already running.

In this exercise you will run commands and observe the output carefully. After each step your instructor will launch a **Kahoot question** based on exactly what you see on screen.

Read every output before moving to the next step — the answer is always in the terminal.

Confirm the cluster is ready:

```
kubectl get nodes
```{{exec}}
