# Well done

You have observed and answered questions on five key aspects of Pod behaviour:

- The API schema: which fields are required in a container definition
- How kubectl reports YAML indentation errors
- Pod lifecycle phases and how to query them directly
- How Pod IPs are assigned from the node's podCIDR range
- How to target a specific container with `-c` in `exec` and `logs`

These are the everyday tools for working with Pods in any Kubernetes cluster.
