# Exercise complete

You have independently reproduced the full storage scenario — from data loss to persistence.

You can now explain why the SQLite file was lost in the first scenario (ephemeral container layer) and why it survived in the second (PersistentVolume decoupled from the Pod lifecycle).

In the IoT lessons ahead, this same telemetry service will receive data from MQTT sensors — the storage layer will work exactly the same way.
