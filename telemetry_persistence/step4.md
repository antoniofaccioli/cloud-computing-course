# Step 4 — Create the PVC and deploy with persistent storage

Create the PersistentVolumeClaim and verify it is bound. Then deploy the service with persistent storage and wait for it to be ready.

---

**Commands available for this step:**

- `kubectl apply`
- `kubectl get pvc`
- `kubectl get pv`
- `kubectl rollout status`
