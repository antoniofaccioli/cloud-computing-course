# Exercise complete

You have explored the cluster and answered questions based on real command output.

The components you have observed — kube-apiserver, etcd, kube-scheduler, kube-controller-manager, kubelet — are the same ones you will configure and interact with in every Kubernetes exercise from this point forward.
