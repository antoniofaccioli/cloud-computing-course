## Exercise complete

You have covered the three main production concerns from this lesson:

- An MLlib Pipeline was trained, evaluated, and saved as a self-contained PipelineModel
- The model was reloaded and applied to new data without any knowledge of the training stages
- A broadcast join eliminated shuffle overhead on a small lookup table, and salting demonstrated how to distribute a skewed key across multiple partitions without changing the output
